{
  "name": "wfc-monorepo-root",
  "private": true,
  "description": "Monorepo containing a Vue app and a TS library",
  "scripts": {
    "dev": "pnpm -filter vue-app dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm run clean:ts && pnpm -r --parallel typecheck",
    "clean:ts": "find . -name '*.tsbuildinfo' -delete"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}